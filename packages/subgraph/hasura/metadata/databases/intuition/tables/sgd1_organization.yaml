table:
  name: organization
  schema: sgd1
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: organization
  custom_root_fields:
    select: organizations
    select_by_pk: organization
select_permissions:
  - role: anonymous
    permission:
      columns:
        - vid
        - block_range
        - atom_id
        - description
        - email
        - id
        - identifier
        - image
        - name
        - url
      filter: {}
      limit: 100
      allow_aggregations: true
    comment: ""

table:
  name: redemption
  schema: sgd1
configuration:
  column_config:
    block$:
      custom_name: block_
  custom_column_names:
    block$: block_
  custom_name: redemption
  custom_root_fields:
    select: redemptions
    select_by_pk: redemption
object_relationships:
  - name: receiver
    using:
      manual_configuration:
        column_mapping:
          receiver_id: id
        insertion_order: null
        remote_table:
          name: account
          schema: sgd1
  - name: sender
    using:
      manual_configuration:
        column_mapping:
          sender_id: id
        insertion_order: null
        remote_table:
          name: account
          schema: sgd1
  - name: vault
    using:
      manual_configuration:
        column_mapping:
          vault_id: id
        insertion_order: null
        remote_table:
          name: vault
          schema: sgd1

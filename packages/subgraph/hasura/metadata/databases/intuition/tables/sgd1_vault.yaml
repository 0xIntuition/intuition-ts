table:
  name: vault
  schema: sgd1
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: vault
  custom_root_fields:
    select: vaults
    select_by_pk: vault
object_relationships:
  - name: atom
    using:
      manual_configuration:
        column_mapping:
          atom_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1
array_relationships:
  - name: deposits
    using:
      manual_configuration:
        column_mapping:
          id: vault_id
        insertion_order: null
        remote_table:
          name: deposit
          schema: sgd1
  - name: positions
    using:
      manual_configuration:
        column_mapping:
          id: vault_id
        insertion_order: null
        remote_table:
          name: position
          schema: sgd1
  - name: redemptions
    using:
      manual_configuration:
        column_mapping:
          id: vault_id
        insertion_order: null
        remote_table:
          name: redemption
          schema: sgd1
select_permissions:
  - role: anonymous
    permission:
      columns:
        - vid
        - block_range
        - total_shares
        - atom_id
        - id
        - triple_id
      filter: {}
      limit: 100
      allow_aggregations: true
    comment: ""

table:
  name: event
  schema: sgd1
configuration:
  column_config:
    block$:
      custom_name: block_
  custom_column_names:
    block$: block_
  custom_name: events
  custom_root_fields:
    select_by_pk: event
object_relationships:
  - name: atom
    using:
      manual_configuration:
        column_mapping:
          atom_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1

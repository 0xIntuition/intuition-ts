table:
  name: triple
  schema: sgd1
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: triple
  custom_root_fields:
    select: triples
    select_by_pk: triple
object_relationships:
  - name: counterVault
    using:
      manual_configuration:
        column_mapping:
          counter_vault_id: id
        insertion_order: null
        remote_table:
          name: vault
          schema: sgd1
  - name: creator
    using:
      manual_configuration:
        column_mapping:
          creator_id: id
        insertion_order: null
        remote_table:
          name: account
          schema: sgd1
  - name: object
    using:
      manual_configuration:
        column_mapping:
          object_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1
  - name: predicate
    using:
      manual_configuration:
        column_mapping:
          predicate_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1
  - name: subject
    using:
      manual_configuration:
        column_mapping:
          subject_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1
  - name: vault
    using:
      manual_configuration:
        column_mapping:
          vault_id: id
        insertion_order: null
        remote_table:
          name: vault
          schema: sgd1

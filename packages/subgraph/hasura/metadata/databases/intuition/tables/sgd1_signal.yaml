table:
  name: signal
  schema: sgd1
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: signal
  custom_root_fields:
    select: signals
    select_by_pk: signal
object_relationships:
  - name: account
    using:
      manual_configuration:
        column_mapping:
          account_id: id
        insertion_order: null
        remote_table:
          name: account
          schema: sgd1
  - name: atom
    using:
      manual_configuration:
        column_mapping:
          atom_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1
  - name: deposit
    using:
      manual_configuration:
        column_mapping:
          deposit_id: id
        insertion_order: null
        remote_table:
          name: deposit
          schema: sgd1
  - name: redemption
    using:
      manual_configuration:
        column_mapping:
          redemption_id: id
        insertion_order: null
        remote_table:
          name: redemption
          schema: sgd1
  - name: triple
    using:
      manual_configuration:
        column_mapping:
          triple_id: id
        insertion_order: null
        remote_table:
          name: triple
          schema: sgd1

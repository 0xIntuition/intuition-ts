table:
  name: account
  schema: sgd1
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: account
  custom_root_fields:
    select: accounts
    select_by_pk: account
object_relationships:
  - name: atom
    using:
      manual_configuration:
        column_mapping:
          atom_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: sgd1

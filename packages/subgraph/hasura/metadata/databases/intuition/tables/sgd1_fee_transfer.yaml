table:
  name: fee_transfer
  schema: sgd1
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: feeTransfer
  custom_root_fields:
    select: feeTransfers
    select_by_pk: feeTransfer
object_relationships:
  - name: receiver
    using:
      manual_configuration:
        column_mapping:
          receiver_id: id
        insertion_order: null
        remote_table:
          name: account
          schema: sgd1
  - name: sender
    using:
      manual_configuration:
        column_mapping:
          sender_id: id
        insertion_order: null
        remote_table:
          name: account
          schema: sgd1
select_permissions:
  - role: anonymous
    permission:
      columns:
        - amount
        - block_number
        - block_timestamp
        - id
        - receiver_id
        - sender_id
        - transaction_hash
        - vid
      filter: {}
      limit: 100
      allow_aggregations: true
    comment: ""
